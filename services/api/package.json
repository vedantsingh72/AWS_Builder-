{
  "name": "@ai-office/api",
  "version": "0.1.0",
  "description": "Ashutosh + Vedant graph-linked modules — every domain table lives here, only here.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "dev": "node ./dist/index.js",
    "db:generate": "prisma generate",
    "db:push": "prisma db push"
  },
  "dependencies": {
    "@ai-office/shared-types": "0.1.0",
    "@prisma/client": "^6.7.0",
    "express": "^4.21.2",
    "socket.io": "^4.8.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.7.4",
    "prisma": "^6.7.0",
    "typescript": "^5.5.4"
  }
}
