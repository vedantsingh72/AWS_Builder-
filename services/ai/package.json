{
  "name": "@ai-office/ai",
  "version": "0.1.0",
  "description": "Vedant — every LLM call, the graph. Writes to domain DB only via client.ts → HTTP → api.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "verify:t1": "node ./scripts/verify-t1.mjs"
  },
  "dependencies": {
    "@ai-office/shared-types": "0.1.0",
    "@langchain/core": "^1.1.30",
    "@langchain/groq": "^1.2.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.5.4"
  }
}
